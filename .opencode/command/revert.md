---
description: Safely revert a Conductor track, phase, or task by reconciling git history with the shared registry and plan state
agent: build
---

@conductor/tracks.md
@conductor/tracks/$ARGUMENTS/plan.md

`$ARGUMENTS` is the track ID generated by `/conductor/new-track` — a `<shortname>_YYYYMMDD` slug (e.g. `user-auth_20260728`) as listed in `conductor/tracks.md`, not a free-form description.

Analyze git history plus `conductor/tracks.md` and the target track's `plan.md` to revert a track, phase, or task safely, resetting checklist state from `[x]` back to `[ ]` where appropriate.
